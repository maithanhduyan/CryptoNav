{
    "configurations": [
        {
            "command": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
            "name": "Server start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}